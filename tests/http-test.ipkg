package http-test

authors = "octeep"

modules = AuthorizationTest, ClientTest, DecompressionTest, QueueTest

depends = base      >= 0.5.1
        , base64
        , contrib   >= 0.5.1
        , network   >= 0.5.1
        , sop       >= 0.5.0
        , elab-util >= 0.5.0
        , tls       >= 0.0.1
        , http      >= 0.0.1

main = Test
executable = runtests

sourcedir = "src"
